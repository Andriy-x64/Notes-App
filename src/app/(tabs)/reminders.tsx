/**
 * Головна точка входу для вкладки нагадувань.
 * Відображає екран списку всіх запланованих нагадувань для нотаток.
 */
import { RemindersScreen } from "@/features/reminders/screens/reminders-screen";

export default function TabRemindersScreen() {
  return <RemindersScreen />;
}
